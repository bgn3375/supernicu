// src/lib/{feature}FilterOptions.ts
// Inline enum-option lists for filter selects — shape is { value; label }[] with `as const`.
// Distinct from FilterOption ({ Id; Text }) which is for entity-selector combos like PersonCombobox.

import type { InvoiceStatus } from '@/types';

export const INVOICE_STATUS_OPTIONS: ReadonlyArray<{ value: InvoiceStatus; label: string }> = [
  { value: 'draft', label: 'Draft' },
  { value: 'sent', label: 'Sent' },
  { value: 'paid', label: 'Paid' },
  { value: 'overdue', label: 'Overdue' },
  { value: 'cancelled', label: 'Cancelled' },
] as const;
